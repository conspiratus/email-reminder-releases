## 1.0.40

- Added explicit PyCharm Claude session and workspace arguments to the NHI delegation helper, so task context is not lost when Claude Code does not export its session ID as an environment variable.
